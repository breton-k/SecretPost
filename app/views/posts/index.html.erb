<center><br>
<!--<%= link_to 'New Post', new_post_path, :class => 'btn btn-primary' %><br><br> -->

  <body>
    <% @posts.each do |post| %>
      <div class="well well-lg">
        <td><%= post.body %></td><br><br>
        <td><%= post.created_at %></td><br>
        <td><%= link_to "Comment (#{post.comments.count})", post %></td>
<!--         <td><%= link_to 'Edit', edit_post_path(post) %></td>
        <td><%= link_to 'Destroy', post, method: :delete, data: { confirm: 'Are you sure?' } %></td> -->
        </div>
    <% end %>
  </body>

<%= will_paginate @posts %>
<br>

</center>

